To compile:
gcc -Wall -O -o Lab11 Lab11.c

To run:
./Lab11

The program prompts the user for a command, creates a child process to
execute it, and displays the output to the terminal and prompts the user
for a new command. To quit the program, enter the command "quit".